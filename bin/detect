#!/usr/bin/env bash

# We always want to run our own buildpack. This way we can error out noticeably and dramatically if we
# find ourselves in an unexpected environment/state.

echo "heroku-buildpack-exp"
exit 0
